// material-ui
import { Container } from '@mui/material';

// ================================|| USERS ||================================ //

const LoginForm = () => {

    return (
        <Container>
            Login
        </Container>
    );
};

export default LoginForm;